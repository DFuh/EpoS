'''
main loop

- loop trough signal-dataset:
-- input df
-- clc
-- output df

'''

### loop while no error and conuter < len(dataset)

# get Date, Pin
# make time_range
# make df ? <-> dataclass
# calc
# store values contained in dataclass
# - if not parameter.select_stored_values -> store all (faster ?)
# - else: store selected values
